# 읽어보기

- 원문 저장소: `microsoft/autogen`
- 미러 저장소: `martinlee-git/autogen`
- 원문 문서: https://github.com/microsoft/autogen/blob/main/dotnet/nuget/README.md
- 미러 경로: `dotnet/nuget/README.md`

## 한글 요약

NuGet 디렉터리 이 디렉터리에는 AutoGen.NET SDK를 NuGet 패키지로 패키징하기 위한 리소스와 메타데이터가 포함되어 있습니다. 파일 icon.png : NuGet 패키지에 사용되는 아이콘입니다. NUGET.md : NuGet 패키지 페이지에 표시되는 추가 정보 파일입니다. NUGET PACKAGE.PROPS : NuGet 패키지에 대한 패키징 설정을 정의하는 MSBuild 속성 파일입니다. 목적 이 디렉터리의 파일은 AutoGen.NET SDK용 NuGet 패키지를 구성하고 빌드하는 데 사용되며, 필요한 메타데이터, 설명서 및 리소스가 포함되어 있는지 확인합니다.

## 핵심 발췌

NuGet 디렉터리 이 디렉터리에는 AutoGen.NET SDK를 NuGet 패키지로 패키징하기 위한 리소스와 메타데이터가 포함되어 있습니다. ## 파일 icon.png : NuGet 패키지에 사용되는 아이콘입니다. NUGET.md : readme 파일 disp

## 원문 내용

# NuGet Directory

This directory contains resources and metadata for packaging the AutoGen.NET SDK as a NuGet package.

## Files

- **icon.png**: The icon used for the NuGet package.
- **NUGET.md**: The readme file displayed on the NuGet package page.
- **NUGET-PACKAGE.PROPS**: The MSBuild properties file that defines the packaging settings for the NuGet package.

## Purpose

The files in this directory are used to configure and build the NuGet package for the AutoGen.NET SDK, ensuring that it includes necessary metadata, documentation, and resources.